<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.teamlab.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:name=".ExampleApplication"
        android:label="@string/app_name"
        android:theme="@style/CustomTheme.NoTitleBar">
        <activity
            android:name=".MainActivity"
            android:theme="@style/CustomTheme.ActionBar"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ModalActivity"   android:theme="@style/CustomTheme.ActionBar.Modal"></activity>
    </application>

</manifest>
